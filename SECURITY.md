# 安全策略

## 支持的版本

| 版本 | 是否支持 |
|---|---|
| `main` 分支最新提交 | ✅ 支持 |
| 历史 tag | ❌ 不单独修补（静态展示站点，不适用多版本并行维护） |

## 报告漏洞

**请不要开公开 issue 报告安全问题。**

优先方式（推荐）：使用 GitHub 私密漏洞报告
→ 仓库 **Security → Report a vulnerability**。仅有维护者可见，可在其中协作修补并申请 CVE。

备选方式：邮件发送至 `3311766878@qq.com`，标题以 `[SECURITY]` 开头，并尽量包含：

- 问题类型（如 XSS、开放重定向、信息泄露）
- 涉及的文件与行号
- 复现步骤与影响范围
- 你建议的修复方式（可选）

## 响应预期

| 阶段 | 时限 |
|---|---|
| 确认收到 | 3 个工作日内 |
| 初步评估与答复 | 7 个工作日内 |
| 修复并发布 | 视严重程度，P0 目标 7 天内 |

## 本仓库的特别提示

1. **站点内容为演示数据**。页面中的人物、新闻、联系方式均为占位信息，请勿替换为任何真实个人身份信息。
2. **第三方 CDN 依赖**。站点通过 CDN 引入 Tailwind CSS 与 Font Awesome，未启用 SRI（Subresource Integrity）。
   这是一项已知取舍：CDN 被投毒会直接影响页面。若部署环境对供应链安全有要求，应先改为本地构建产物 + SRI。
3. **请勿提交密钥**。仓库中不应出现 API Key、令牌、真实邮箱密码等；`.gitignore` 已排除 `.env*`。

## 致谢

报告者如希望公开致谢，会在修复的 Release Notes 中列出；否则默认匿名。
